source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '6.0'

def all_pods
  pod 'SAMKeychain', '~> 1.5.2'
  pod  'tolo', '~> 1.0'
end
target 'VENTouchLock' do
  all_pods
end

target 'VENTouchLockTests' do
  all_pods
  pod 'Specta', '~> 0.2.1'
  pod 'Expecta', '~>0.2'
  pod 'OCMock', '~>2.2.2'
end
